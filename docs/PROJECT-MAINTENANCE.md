# 工程归档与当前分支

2026-09-10 按用户要求检查整个工程，先归档过程文件，再固化并推送 `dev` 和 `web-deploy`。当前产品保留基础演奏流程和性能、录音、部署修复；此前取消的分段练习、练习历史、慢练和挑战不恢复。

手机“关于”入口修复为 `e04fd90`，只调整首页窄屏导航样式。已检查 320px、390px 和 1440px 视口；手机按钮高度 44px，弹窗可打开和关闭。阿里云已发布 `20260909T173023Z-d74e6e1e-4c535da6`，实际 HTTPS 手机页面验收通过。

## 本地归档

位置：`D:\LLM\_work\Syrinx\2026-09-10-project-cleanup`。

- `files/`：按原工程相对路径归档的旧任务日志、会话检查脚本、一次性诊断脚本、截图、临时分析数据、历史 OMR 工作产物、背景生成过程和已发布候选包。
- `archive-plan.json`：174 个归档项及各自用途、原路径和目标位置。
- `files-sha256.json`：578 个普通文件的 SHA256，合计 368,760,712 字节；迁移后逐项核验一致。数量不包含下列工作区内部文件。
- `preserved-assets.json`：42 个正式素材和测试基准文件的校验记录，整理前后未变。
- `move-journal.txt`：实际迁移记录。
- `worktrees/`：通过 `git worktree move` 迁移的工作区，提交位置及原有未提交修改均保留。

| 分支 | 当前工作位置 | 状态 |
|---|---|---|
| `dev` | `D:\Syrinx` | 日常开发；localhost 5173 |
| `web-deploy` | 归档根目录下 `worktrees/web-deploy` | 四曲部署配置，继续用于发布 |
| `codex/syrinx-r1` | `worktrees/r1-history` | 历史实现记录 |
| `codex/syrinx-r2` | `worktrees/r2-cancelled` | 已撤回，不继续实施 |
| `codex/syrinx-r3` | `worktrees/r3-cancelled` | 已取消，含四个未提交修改文件，不发布 |

这些工作区仍由主工程的 `.git` 管理；如需再次移动，应使用 `git worktree move`，不要直接改目录名。需要恢复普通归档文件时，按清单复制回原相对路径，先检查目标是否已有新版本。

## 保留内容与后续约定

保留全部应用源码、单元测试、发布工具及测试、可复用素材处理/校验脚本、README 引用的图片、项目文档、`.claude` 配置、`app/node_modules`、原始曲谱/音频/模型及 `app/public/songs`。特别保留 `docs/需求与调研/曲谱解析/luv_letter_timeline.json`，它是现有时间轴交叉测试直接读取的基准。

历史计划中的过程截图/一次性脚本路径可能已迁入归档；这些文字保留为历史记录，不能当作当前自动化入口。新验收截图、日志和临时发布包放在外部工作目录，避免重新堆积到源码工程。正式素材不推送 GitHub；只推送已验证的 `dev`、`web-deploy`，不推送取消的实验分支。
