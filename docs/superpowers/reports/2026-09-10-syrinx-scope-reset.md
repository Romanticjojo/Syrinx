# Syrinx 功能范围恢复记录

2026-09-10（香港时间），用户明确要求停止并撤掉分段练习及练习类新增功能。此决定取代此前 R2/R3 实施计划；后续不得从历史计划自动恢复这些功能。

## 当前范围

恢复原来的选曲、预览、演奏、录音和回放流程。保留 R1 的伴奏预加载、页面延迟加载、演奏/录音会话隔离、实际采集区间评分和版本化部署优化。

分段循环、持久练习记录、薄弱小节重练、成绩比较已从 dev/web-deploy 移除。R3 的保音高慢练、时间映射、起音参考和挑战已停止，未合入这两个分支、未发布。其独立实验分支和未完成修改保留，不继续执行。5174、5175、5177 测试服务及对应测试页面已关闭。

## 恢复证据

- dev 恢复提交 `cb3f61d`，应用文件与 R2 前的 `853a41b` 完全一致。
- web-deploy 合并提交 `28d9cda`，应用文件与 R2 前的 `76ae8d2` 完全一致，保留部署版四曲配置。
- 两个分支分别通过 39 个文件、320 项测试及生产构建。构建保留已有大型谱面包提示；未新增功能或镜像测试。
- 阿里云重新校验保留的 R1 发布包，四首优化媒体和全部 63 个清单文件通过校验。发布器通过 8090 和 HTTPS 资源检查后，将入口原子恢复到 `20260909T143557Z-bf1add9e-49b59e89`。
- 恢复前的 R2 首页备份：`/var/www/syrinx/.index-history/20260909T172001.128480Z-before-20260909T143557Z-bf1add9e-49b59e89.html`。保留版本文件，未清除浏览器本地数据或修改私人媒体。
- 实际浏览器刷新确认线上加载 R1 版本脚本；本地和线上预览、演奏页无分段、轮次、历史、慢练或挑战入口。线上实际录制约 25 秒，暂停计时冻结，停止进入原回放与音准统计页面。
- 主域名、www 和同机 Alnazar 均返回 HTTP 200。未修改其他服务及腾讯云。

本地开发服务继续运行在 http://127.0.0.1:5173/，正式网址 https://romanticjojo.com/。浏览器录音仅验证流程；没有以真实长笛演奏验证评分准确性。
