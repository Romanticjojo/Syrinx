# 2026-09-13 界面收尾与发布验证

## 本次变化

- 网页个人仓库入口显示「开发中」，弹窗只保留一句说明和「知道了」。
- 删除曲库页两句宣传文案、同步调试链接，以及曲目预览底部的素材路径说明。
- BPM 按钮使用 13px、500 字重；数值面板改为 32px 无衬线字。触控区域仍至少 44px。
- 首次演奏提示选小节、调整 BPM。关闭或成功开始倒数后记住选择；存储不可用时不阻断演奏。
- 起点选中框保留到该小节的伴奏结束锚点，只在实际播放时清除。暂停定位、继续演奏和最后一小节均覆盖。
- 中英文 README 恢复 Archify 架构图、数据流图，并更新实际播放时钟、录音分段和后台分析说明。

## 自动检查

- 网页模式：58 个测试文件、571 项测试通过。
- 本地功能模式：演奏页、选小节、网页入口和个人仓库集合的 77 项定向测试通过。
- 网页、本地功能模式的 TypeScript 与生产构建通过。
- 静态检查无错误；保留原有 13 条警告。
- 发布脚本 17 项测试通过。
- 新增 4 项行为测试先失败、实现后通过；包括首次提示记忆和选中框消失的精确边界。

## Computer Use（Codex 内置浏览器）

- 曲库 → 个人仓库提示 → 关闭 → 曲目预览 → 演奏，操作正常。
- Luv Letter 就绪时从 100 调至 80 BPM，保持停止；点击第 5 小节后从 0:11 起奏。
- 演奏越过第 5 小节，选中框消失。暂停后改选第 3 小节，继续时框仍在，经过该小节后消失。
- 实际生成两段录音（第 5–14 小节和第 3–14 小节）；回放、分段切换与对照伴奏播放可用，结果标注 80% 速度。
- 音高分析在后台显示进度时，仍可启动对照回放。未使用真人长笛演奏评判音准。
- 再次进入演奏页不重复显示首次提示。
- 320×640 手机上的速度弹窗完整可见，宽度 300px；字大小为按钮 13px、输入框 32px。
- 320、390、540、768、820、1024px 视口无横向溢出，六个底部按钮均未超出屏幕且高度为 44px。

## README 图源

四份 Archify HTML 通过 9/9 showcase 确定性检查，PNG 图进行了视觉核对。保留原版式并修正右侧回放连线的裁切。架构图在常见桌面尺寸下可完整显示；内容较多的数据流交互图保留纵向阅读，README 使用完整 PNG。未通过其他浏览器自动化方式采集图表证据。

GitHub 仓库继续保持私有；本次不创建公开仓库，不更改可见性。
