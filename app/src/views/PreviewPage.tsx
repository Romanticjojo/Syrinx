export default function PreviewPage() {
  return (
    <main style={{ display: 'grid', placeItems: 'center', height: '100%' }}>
      <p style={{ color: 'var(--ink-dim)' }}>预览页（任务 2 实现）</p>
    </main>
  )
}
