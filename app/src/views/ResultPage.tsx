export default function ResultPage() {
  return (
    <main style={{ display: 'grid', placeItems: 'center', height: '100%' }}>
      <p style={{ color: 'var(--ink-dim)' }}>回放页（任务 12 实现）</p>
    </main>
  )
}
